# Internationalisation (i18n) Skill

## Stack (already installed)

- `symfony/translation` — translation component
- `symfony/intl` — locale-aware date/number formatting

## Config

```yaml
# config/packages/translation.yaml
framework:
    default_locale: fr
    translator:
        default_path: '%kernel.project_dir%/translations'
        fallbacks: [fr]
```

## Translation files — YAML format

```yaml
# translations/messages.fr.yaml
report:
    lost:
        title: "Signaler un animal perdu"
        submit: "Publier le signalement"
    found:
        title: "Signaler un animal trouvé"
    status:
        created: "En attente"
        published: "Publié"
        matched: "Correspondance trouvée"
        resolved: "Résolu"
        expired: "Expiré"
        closed: "Fermé"
    results: >
        {count, plural,
            =0    {Aucun résultat}
            one   {1 résultat}
            other {# résultats}
        }

nav:
    home: "Accueil"
    reports: "Signalements"
    new_report: "Nouveau signalement"
    login: "Connexion"
    logout: "Déconnexion"
```

## Twig usage

```twig
{# Simple translation #}
{{ 'report.lost.title'|trans }}

{# With parameters #}
{{ 'report.results'|trans({'count': results.total}) }}

{# Date formatting (Intl) #}
{{ report.createdAt|format_date('long', locale='fr') }}
{# → "10 mai 2026" #}

{# Relative time #}
{{ report.createdAt|format_datetime('relative_short', locale='fr') }}
{# → "il y a 2 jours" #}
```

## PHP usage

```php
use Symfony\Contracts\Translation\TranslatorInterface;

public function __construct(private readonly TranslatorInterface $t) {}

$message = $this->t->trans('report.results', ['count' => $count]);
```

## Domain Enums with labels

```php
enum Species: string {
    case DOG   = 'dog';
    case CAT   = 'cat';
    case BIRD  = 'bird';
    case OTHER = 'other';

    public function label(): string {
        return match($this) {
            self::DOG   => 'Chien',
            self::CAT   => 'Chat',
            self::BIRD  => 'Oiseau',
            self::OTHER => 'Autre',
        };
    }
}

// In Form: 'choice_label' => fn(Species $s) => $s->label()
// In Twig: {{ report.species.label() }}
```

## Translation domains

| Domain | Usage |
|---|---|
| `messages` | General UI |
| `validators` | Validation errors |
| `security` | Auth / login |
| `emails` | Email subjects and bodies |

```yaml
# translations/validators.fr.yaml
"This value should not be blank.": "Ce champ est obligatoire."
"This value is not a valid email address.": "Adresse email invalide."
"This value is too short. It should have {{ limit }} character or more.|This value is too short. It should have {{ limit }} characters or more.": "Ce champ doit contenir au moins {{ limit }} caractères."
```

## Rules

- Default locale = `fr` — this is a French platform
- All user-facing strings in translation files — never hard-coded in PHP/Twig
- ICU message format for plurals — never string concatenation
- Date/number formatting via `symfony/intl`, never `date()` PHP function
- Translation keys: `domain.context.label` (e.g. `report.status.published`)
- Enum labels defined directly on the Enum as `label()` method, not in translation files
